---
title: "WD11 Analysis"
author: "Claude Cugerone"
date: "2025-06-24"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# GLOBAL VARIABLES
```{r Variables}
# Si besoin de changer de déploiement, Ctrl+F sur SealOfApproval
ROOT_PROJ <- "/home/cactus/Documents/Oceano/M2/LOCEAN/Weddell_seal_fluo"

DATA_FOLDER_CORRECTED <- "oceanographic_data_corrected"
DEPLOY_FOLDER <- "wd11" # Lignes à modifier pour changer de dépoliement... SealOfApproval

DEPLOYMENT_FILE <- paste0("combined_",DEPLOY_FOLDER,".rds")
RDS_DEPLOYMENT_PATH <- file.path(ROOT_PROJ, DATA_FOLDER_CORRECTED, DEPLOY_FOLDER, DEPLOYMENT_FILE)
RDS_ALL <- file.path(ROOT_PROJ, DATA_FOLDER_CORRECTED, "all_deployment.rds")
# Pour les cartes
BATHY_FOLDER <- "bathy"
FINE_TOPO_FILE <- "gvdem100v3/w001001.adf"
LARGE_TOPO_FILE <- "gebco_2023_n-50.0_s-79.0_w90.0_e180.0.tif"
BATHY_PATH_FINE <- file.path(ROOT_PROJ, BATHY_FOLDER, FINE_TOPO_FILE)
BATHY_PATH_LARGE <- file.path(ROOT_PROJ, BATHY_FOLDER, LARGE_TOPO_FILE)

FAST_ICE_FOLDER <- "fast_ice"
FAST_ICE_FILE <- "mertz_sara_akiko_20.nc" # Lignes à modifier pour changer de deployment SealOfApproval
FAST_ICE_PATH <- file.path(ROOT_PROJ, FAST_ICE_FOLDER, FAST_ICE_FILE)

AMSR_FOLDER <- "amsr2"
AMSR_FOLDER_YEAR <- "2020" # Lignes à modifier pour changer de deployment SealOfApproval
AMSR_FOLDER_PATH <- file.path(ROOT_PROJ,AMSR_FOLDER,AMSR_FOLDER_YEAR)
```

#HISTO
## Histogram with all individuals (light and Chla)
```{r}
library(dplyr)
library(ggplot2)
library(lubridate)

# Charger les données
all_seals_data <- readRDS(RDS_DEPLOYMENT_PATH)

# Préparer les données avec mois
all_seals_data <- all_seals_data %>%
  mutate(
    month = floor_date(date, "month")
  )

# Résumer pour light_raw
light_counts <- all_seals_data %>%
  filter(!is.na(light_raw)) %>%
  group_by(seal_id, month, profile_id) %>%
  summarise(.groups = "drop") %>%
  group_by(seal_id, month) %>%
  summarise(n_profiles = n(), .groups = "drop") %>%
  mutate(type = "Light")

# Résumer pour chla_raw
chla_counts <- all_seals_data %>%
  filter(!is.na(chla_raw)) %>%
  group_by(seal_id, month, profile_id) %>%
  summarise(.groups = "drop") %>%
  group_by(seal_id, month) %>%
  summarise(n_profiles = n(), .groups = "drop") %>%
  mutate(type = "Fluorescence")

# Combiner les deux
combined_counts <- bind_rows(light_counts, chla_counts)
# pour mettre les facets dans un ordre souhaité
combined_counts <- combined_counts %>%
  mutate(type = factor(type, levels = c("Light", "Fluorescence")))

# Créer une étiquette propre pour l’axe x
combined_counts <- combined_counts %>%
  mutate(
    month_label = format(month, "%b %Y"),
    month_label = factor(month_label, levels = unique(format(sort(unique(month)), "%b %Y")))
  )

# Plot : 1 facet pour Light, 1 pour Fluorescence, individus en couleur
ggplot(combined_counts, aes(x = month_label, y = n_profiles, fill = seal_id)) +
  geom_col(position = "dodge") +
  facet_wrap(~type, ncol = 1, scales = "free_y") +
  theme_minimal() +
  labs(
    title = "Number of profiles (light and fluo) per individual per month",
    x = "",
    y = "Number of profiles",
    fill = "Seal"
  ) +
  theme(axis.text.x = element_text(angle = 45, hjust = 1)) +
  scale_fill_brewer(palette = "Set3")




```
# TIME SERIES
## Time series of all individuals with map
```{r}
library(gsw)
library(ggplot2)
library(dplyr)
library(tidyr)
library(patchwork)

# Charger les données
all_seals_data <- readRDS(RDS_DEPLOYMENT_PATH)

# Paramètres
limits_list <- list(
  "light" = range(all_seals_data$light_corrected, na.rm=T),
  "chla" = range(all_seals_data$chla_corrected, na.rm=T),
  "temp" = range(all_seals_data$temperature, na.rm=T),
  "sal" = c(33,36),
  "dens" = c(26, 28)
)

# Liste pour stocker les graphiques finaux
all_seal_plots <- list()

# Boucle sur chaque individu
for (seal_id in unique(all_seals_data$seal_id)) {
  data_one_seal <- all_seals_data %>% filter(seal_id == !!seal_id)
  
  SEAL_NAME <- seal_id

# Format long
data_long <- data_one_seal %>%
  filter(!(is.na(light_corrected) & is.na(chla_corrected) &
           is.na(temperature) & is.na(salinity))) %>%
  mutate(sigma0 = gsw_sigma0(salinity, gsw_CT_from_t(salinity, temperature, 0))) %>%
  dplyr::select(date, depth, light_corrected, chla_corrected, temperature, salinity, sigma0) %>%
  pivot_longer(cols = -c(date, depth), names_to = "variable", values_to = "value") %>%
  mutate(variable = recode(variable,
                           temperature = "temp",
                           salinity = "sal",
                           sigma0 = "dens",
                           light_corrected = "light",
                           chla_corrected = "chla")) %>%
  mutate(variable = factor(variable, levels = c("light", "chla", "temp", "sal", "dens")))
  
  # Générer les 5 graphiques
  plots <- data_long %>%
    group_split(variable) %>%
    lapply(function(df) {
      var <- unique(df$variable)
      lims <- limits_list[[var]]
      depth <- c(200,0)

      ggplot(df, aes(x = date, y = depth, fill = value)) +
        geom_tile() +
        scale_y_reverse(limits = depth) +
        scale_x_datetime(date_breaks = "1 month", date_labels = "%b-%Y") +
        scale_fill_gradientn(colors = c("blue", "darkgreen", "greenyellow", "red"),
                             limits = lims, name = var, na.value = NA) +
        guides(fill = guide_colorbar(barwidth = 0.5, barheight = 3)) +
        labs(title = paste0(var, " - ", SEAL_NAME), x = "", y = "Depth (m)") +
        theme_minimal(base_size = 9)
    })
  
  # Empilement vertical des 5 variables pour ce phoque
  all_seal_plots[[seal_id]] <- wrap_plots(plots, ncol = 1)
}

for (plot in all_seal_plots) {
  print(plot)
  Sys.sleep(1)
}

```
# LIGHT
## Light depth maximum + light value
```{r}
library(dplyr)
library(ggplot2)
library(lubridate)

# Charger les données
all_seals_data <- readRDS(RDS_DEPLOYMENT_PATH)



# Préparation : pour chaque seal et date, on calcule max lumière et profondeur de max(light)
df_max_light <- all_seals_data %>%
  group_by(seal_id, date = floor_date(date, unit = "hour")) %>%
  filter(!is.na(light_corrected)) %>%
  summarise(
    max_light = max(light_corrected, na.rm = TRUE),
    depth_at_max_light = depth[which.max(light_corrected)],
    .groups = "drop"
  )

year_wd <- unique(year(df_max_light$date))
plot_title <- paste0("Max(light) per profile & associated depth (",year_wd,")")

# Plot pour tous les individus
ggplot(df_max_light, aes(x = date, y = depth_at_max_light, color = max_light)) +
  geom_point(size = 1) +
  facet_wrap(~ seal_id, scales = "free_x") +
  scale_color_viridis_c(
    option = "plasma",
    name = "Depth (m)",
    guide = guide_colorbar(
      barwidth = 0.5,        # réduit la largeur
      barheight = 5,         # ajuste la hauteur (selon orientation)
      ticks.linewidth = 0.5,
      label.theme = element_text(size = 8)
    )) +
  scale_x_datetime(date_breaks = "3 weeks", date_labels = "%b") +
  scale_y_reverse()+
  labs(
    title = plot_title,
    x = "",
    y = "max(light) (µmol/m²/s)"
  ) +
  theme_minimal() +
  theme(
  plot.margin = margin(20, 20, 20, 20),       # marge top, right, bottom, left en pts
  panel.spacing = unit(1, "cm"),              # espace entre facettes 
  axis.text.x = element_text(angle = 45, hjust = 1)
)

```
## Lunar package
```{r}
library(dplyr)
library(tidyr)
library(lubridate)
library(oce)
library(lunar)
library(ggplot2)
library(scales)

# Charger les données
all_seals_data <- readRDS(RDS_DEPLOYMENT_PATH)

# seuil "c'est la nuit"
solar_threshold <- -12

# Fonction pour calculer la moyenne de lumière dans un intervalle
simple_light_avg <- function(depths, lights, min_depth, max_depth) {
  idx <- which(depths >= min_depth & depths <= max_depth)
  mean(lights[idx], na.rm = TRUE)
}

# Traitement par seal_id
df_solar <- all_seals_data %>%
  group_by(seal_id, date = floor_date(date, unit = "hour")) %>%  
  summarise(
    lon = mean(lon, na.rm = TRUE),
    lat = mean(lat, na.rm = TRUE),
    light_surf = simple_light_avg(depth, light_corrected, 4, 10),
    .groups = "drop"
  ) %>%
  mutate(
    lat_bin = floor(lat * 20) / 20,
    lat_bin_label = factor(paste0("[", lat_bin, "°, ", lat_bin + 0.05, "°[")),
    solar_angle = sunAngle(date, lon, lat)$altitude,
    period = case_when(
      solar_angle >= 0 ~ "Day",
      solar_angle < 0 & solar_angle > solar_threshold ~ "Twilight",
      solar_angle <= solar_threshold ~ "Night"
    ),
    lunar_phase = lunar.phase(date, shift = 12, name = TRUE)
  )

# Extraire les dates de pleine lune
full_moon_dates <- df_solar %>%
  filter(lunar_phase == "Full") %>%
  mutate(date_only = as.Date(date)) %>%
  distinct(date_only) %>%
  pull(date_only) %>%
  as.POSIXct(tz = "UTC")

seal_ids <- unique(df_solar$seal_id)

# Palette douce pour les périodes
period_colors <- c("Day" = "#E69F00",      
                   "Twilight" = "#8E44AD",  
                   "Night" = "#001fcf") 

for (seal in seal_ids) {
  df_plot <- df_solar %>% filter(seal_id == seal)
  
  # Créer un dataframe long avec variable et valeur, pour faire un seul geom_point()
  df_long <- df_plot %>%
    dplyr::select(date, period, light_surf, solar_angle) %>%
    pivot_longer(cols = c(light_surf, solar_angle), names_to = "variable", values_to = "value") %>%
    mutate(
      variable = recode(variable,
                        light_surf = "Light",
                        solar_angle = "Solar Angle")
    )
  
  # Vérifie qu'il y a assez de données
  if (sum(!is.na(df_long$value)) >= 2) {
    
    p <- ggplot(df_long, aes(x = date, y = value, color = period, shape = variable)) +
      geom_point(size = 1.5) +
      geom_vline(xintercept = full_moon_dates, linetype = "dashed", color = "grey40") +
      scale_color_manual(values = period_colors, name = "Period") +
      scale_shape_manual(values = c("Light" = 16, "Solar Angle" = 4), name = "Variable") +
      scale_x_datetime(date_breaks = "2 weeks", date_labels = "%d-%m-%Y") +
      labs(
        title = paste("Light Surface and Solar Angle -", seal),
        y = "Value",
        x = ""
      ) +
      theme_minimal(base_size = 14) +
      theme(legend.position = "bottom") + 
      theme(axis.text.x = element_text(angle = 45, hjust = 1))
    print(p)
  } else {
    message("Pas assez de données pour tracer ", seal)
  }
}

```
## Saving light profiles 
```{r SaveAndClassifyLightProfiles, results = 'hide'}
library(ggplot2)
library(dplyr)
library(tidyr)
library(scales)
library(purrr)

# Charger les données
all_seals_data <- readRDS(RDS_DEPLOYMENT_PATH)

# Préparer les données
df <- all_seals_data %>%
  dplyr::select(profile_id, seal_id, date_simple, depth, light = light_corrected, light_raw, MLD) %>%
  filter(!is.na(light) & !is.na(depth))

# Déterminer les ranges
light_range <- range(c(df$light, df$light_raw), na.rm = TRUE)
depth_range <- rev(range(df$depth, na.rm = TRUE))

# Générer les graphes par profil
df %>%
  group_by(seal_id, profile_id, date_simple) %>%
  group_split() %>%
  walk(function(df_profile) {
    seal <- df_profile$seal_id[1]
    profile <- df_profile$profile_id[1]
    date <- df_profile$date_simple[1]
    
    output_dir <- file.path("oceanographic_data_corrected", DEPLOY_FOLDER, seal, "light_fix_scale")
    if (!dir.exists(output_dir)) dir.create(output_dir, recursive = TRUE)

    if (all(is.na(df_profile$light)) & all(is.na(df_profile$light_raw))) return(NULL)

    p <- ggplot(df_profile) +
      geom_point(aes(x=light, y=depth, color="Corrected"), size=0.5) +
      geom_point(aes(x=light_raw, y=depth, color="Raw"), size=0.5) +
      geom_hline(aes(yintercept = MLD, linetype = "MLD"), color = "purple", linewidth = 0.8) +
      scale_y_reverse(name = "Depth (m)", limits = depth_range) +
      scale_x_continuous(name = "Light (µmol/m²/s)", limits = light_range) +
      scale_color_manual(values = c("Raw" = "blue", "Corrected" = "red")) +
      scale_linetype_manual(
        name = "",
        values = c("MLD" = "dashed")
      ) +
      labs(title = paste(seal, "- Profile n°", profile, " (", date,")")) +
      theme_minimal() +
      theme(
        panel.background = element_rect(fill = "white", color = NA),
        plot.background = element_rect(fill = "white", color = NA)
      )

    filename <- paste0(seal, "_", profile, "_", date, ".png")
    filepath <- file.path(output_dir, filename)
    ggsave(filepath, plot = p, width = 6, height = 5, dpi = 300)
  })

```

# CHLA
## Chla depth maximum
```{r}
library(dplyr)
library(ggplot2)

# Charger les données
all_seals_data <- readRDS(RDS_DEPLOYMENT_PATH)

df_long <- all_seals_data %>%
  filter(!is.na(chla_corrected)) %>%
  dplyr::select(seal_id, profile_id, depth, chla_corrected, date_simple)

df_max <- df_long %>%
  group_by(seal_id, profile_id) %>%
  slice_max(chla_corrected, n = 1, with_ties = FALSE) %>%  # prendre la ligne avec max(chla_corrected)
  ungroup() %>%
  mutate(date_simple = as.Date(date_simple)) # on remet au format Date...

ggplot(df_max, aes(x = date_simple, y = depth, color = seal_id)) +
  geom_point(shape = 19, size= 0.5) +
  geom_smooth(method = "loess", formula = y ~ x, se = FALSE, linewidth = 0.7, span = 0.4, degree = 1, aes(group = seal_id)) +
  scale_y_reverse() +
  labs(
    x = "",
    y = "Depth",
    color = "Seal ID",
    title = "Max Chl-a per Profile over Time"
  ) +
    scale_x_date(
    date_breaks = "2 weeks",
    date_labels = "%Y-%m-%d"
  ) +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```
## Sum of Chla profile per individual
```{r}
library(dplyr)
library(ggplot2)
library(lubridate)

# Charger les données
all_seals_data <- readRDS(RDS_DEPLOYMENT_PATH)

df_long <- all_seals_data %>%
  filter(!is.na(chla_corrected)) %>%
  dplyr::select(seal_id, profile_id, depth, chla_corrected, date_simple)

df_sum <- df_long %>%
  group_by(seal_id, profile_id) %>%
  summarise(
    total_chla = sum(chla_corrected, na.rm = TRUE),
    date_simple = first(date_simple)
  ) %>% 
  ungroup() %>%
  mutate(date_simple = as.Date(date_simple),    
          year = year(date_simple),
          month_day = format(date_simple, "%m-%d"),        
          month_day = as.Date(month_day, format = "%m-%d"))

year_val <- unique(lubridate::year(df_sum$date_simple))

df_sum_filtered <- df_sum %>%
  filter(month(month_day) <= 6) %>%
  filter(!(month(month_day) > 6 & total_chla > 400))

# Convertir les dates en numériques pour calculer les quantiles
date_num <- as.numeric(df_sum_filtered$date_simple)
# Calculer les bornes pour ignorer les extrêmes
qs <- quantile(date_num, probs = c(0.05, 0.95))
df_smooth <- df_sum_filtered %>%
  filter(date_simple >= qs[1], date_simple <= qs[2])

ggplot(df_sum_filtered, aes(x = date_simple, y = total_chla, color = seal_id)) +
  geom_point(shape = 19, alpha = 0.7, size = 0.25) +
  geom_smooth(data = df_smooth, method = "loess", se = FALSE, span = 0.4, degree = 1, linewidth = 0.7) +
  labs(
    x = "",
    y = expression(paste(Sigma, " Chl-a Fluo (mg.m"^{-3},")")),
    color = "Seal ID",
    title = paste0("Sum of Chl-a per Profile (", DEPLOY_FOLDER, " - ", year_val,")")
  ) +
    scale_x_date(
    date_breaks = "1 month",
    date_labels = "%B"
  ) +
  scale_y_continuous(limits = c(0, 400)) +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```
## Sum of Chla profile for 2019 2020 2023
```{r}
library(dplyr)
library(ggplot2)
library(lubridate)

# Charger les données
all_seals_data <- readRDS(RDS_ALL) # tous les individus de toutes les années.

# Mise en forme
df_long <- all_seals_data %>%
  filter(!is.na(chla_corrected)) %>%
  dplyr::select(seal_id, profile_id, depth, chla_corrected, date_simple)

df_sum <- df_long %>%
  group_by(seal_id, profile_id) %>%
  summarise(
    total_chla = sum(chla_corrected, na.rm = TRUE),
    date_simple = first(date_simple),
    .groups = "drop"
  ) %>%
  mutate(
    date_simple = as.Date(date_simple),
    year = year(date_simple),
    month_day = format(date_simple, "%m-%d"),         # extrait mois-jour
    month_day = as.Date(month_day, format = "%m-%d")  
  )

df_sum_filtered <- df_sum %>%
  filter(month(month_day) <= 6) %>%
  filter(!(month(month_day) > 6 & total_chla > 400))

# Convertir les dates en numériques pour calculer les quantiles
date_num <- as.numeric(df_sum_filtered$date_simple)
# Calculer les bornes pour ignorer les extrêmes
qs <- quantile(date_num, probs = c(0.05, 0.95))
df_smooth <- df_sum_filtered %>%
  filter(date_simple >= qs[1], date_simple <= qs[2])

# Plot
ggplot(df_sum_filtered, aes(x = month_day, y = total_chla,
                            color = factor(year), group = interaction(year, seal_id))) +
  geom_point(shape = 19, alpha = 0.7, size = 0.25) +
  geom_smooth(data=df_smooth, method = "loess", formula = y ~ x, se = FALSE, span = 0.5, degree = 1, linewidth = 0.5) +
  labs(
    x = "",
    y = expression(paste(Sigma, " Chl-a Fluo (mg m"^{-3},")")),
    color = ""
  ) +
  scale_x_date(
    date_breaks = "1 month",
    date_labels = "%b"
    ) +
  scale_y_continuous(limits = c(0, 400)) +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))

```
## Mean Chl-a Profile per individual
```{r}

library(dplyr)
library(ggplot2)
library(lubridate)
library(pracma)  # pour trapz()

all_seals_data <- readRDS(RDS_DEPLOYMENT_PATH)

df_long <- all_seals_data %>%
  filter(!is.na(chla_corrected)) %>%
  dplyr::select(seal_id, profile_id, depth, chla_corrected, date_simple)

df_avg <- df_long %>%
  group_by(seal_id, profile_id) %>%
  summarise(
    mean_chla = if(n() > 1) { # si le profil a plus d'un point
      trapz(depth, chla_corrected) / max(depth)  # intégrale / profondeur max
    } else {
      chla_corrected  # si un seul point
    },
    date_simple = first(date_simple)
  ) %>% 
  ungroup() %>%
  mutate(
    date_simple = as.Date(date_simple),    
    year = year(date_simple),
    month_day = format(date_simple, "%m-%d"),        
    month_day = as.Date(month_day, format = "%m-%d")
  )
year_val <- unique(lubridate::year(df_avg$date_simple))

df_avg_filtered <- df_avg %>%
  filter(month(month_day) <= 6)

# Convertir les dates en numériques pour calculer les quantiles
date_num <- as.numeric(df_avg_filtered$date_simple)
# Calculer les bornes pour ignorer les extrêmes
qs <- quantile(date_num, probs = c(0.05, 0.95))
df_smooth <- df_avg_filtered %>%
  filter(date_simple >= qs[1], date_simple <= qs[2])

ggplot(df_avg_filtered, aes(x = date_simple, y = mean_chla, color = seal_id)) +
  geom_point(shape = 19, alpha = 0.7, size = 0.25) +
  geom_smooth(data = df_smooth, method = "loess", se = FALSE, span = 0.4, degree = 1, linewidth = 0.7) +
  labs(
    x = "",
    y = expression(paste("Mean Chl-a (mg.m"^{-3},")")),
    color = "Seal ID",
    title = paste0("Mean Chl-a per Profile (", DEPLOY_FOLDER, " - ", year_val,")")
  ) +
  scale_x_date(
    date_breaks = "1 month",
    date_labels = "%B"
  ) +
  scale_y_continuous(limits = c(0, 4)) +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))


```
## Mean Chl-a Profile for 2019 2020 2023
```{r}
library(dplyr)
library(ggplot2)
library(lubridate)
library(pracma)  # pour trapz()

# Charger les données
all_seals_data <- readRDS(RDS_ALL) 

# Mise en forme
df_long <- all_seals_data %>%
  filter(!is.na(chla_corrected)) %>%
  dplyr::select(seal_id, profile_id, depth, chla_corrected, date_simple)

# Calcul de la moyenne intégrée par profil
df_avg <- df_long %>%
  group_by(seal_id, profile_id) %>%
  summarise(
    mean_chla = if(n() > 1) {
      trapz(depth, chla_corrected) / max(depth)
    } else {
      chla_corrected
    },
    date_simple = first(date_simple),
    .groups = "drop"
  ) %>%
  mutate(
    date_simple = as.Date(date_simple),
    year = year(date_simple),
    month_day = format(date_simple, "%m-%d"),
    month_day = as.Date(month_day, format = "%m-%d")
  )

# Filtrage similaire à ton code original
df_avg_filtered <- df_avg %>%
  filter(month(month_day) <= 6)

# Convertir les dates en numériques pour calculer les quantiles
date_num <- as.numeric(df_avg_filtered$date_simple)
# Calculer les bornes pour ignorer les extrêmes
qs <- quantile(date_num, probs = c(0.05, 0.95))
df_smooth <- df_avg_filtered %>%
  filter(date_simple >= qs[1], date_simple <= qs[2])

# Visualisation : toutes années ensemble, par profil
ggplot(df_avg_filtered, aes(x = month_day, y = mean_chla, group = seal_id)) +
  geom_point(shape = 19, alpha = 0.7, size = 0.25) +
  geom_smooth(data = df_smooth, aes(color = factor(year)), method = "loess", se = FALSE, span = 0.4, degree = 1, linewidth = 0.5) +
  labs(
    x = "",
    y = expression(paste("Mean Chl-a (mg.m"^{-3},")")),
    color = "Year"
  ) +
  scale_x_date(
    date_breaks = "1 month",
    date_labels = "%b"
  ) +
  scale_y_continuous(limits = c(0, 4)) +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))


```


## Saving Chl-a profiles
```{r SaveAndClassifyChlaProfilesFinal, results = 'hide'}

library(ggplot2)
library(dplyr)
library(tidyr)
library(scales)
library(purrr)

# Charger les données
all_seals_data <- readRDS(RDS_DEPLOYMENT_PATH)

# Préparer les données
df <- all_seals_data %>%
  dplyr::select(profile_id, seal_id, date_simple, depth, chla = chla_corrected, chla_raw, MLD) %>%
  filter(!is.na(chla) & !is.na(depth))

# Déterminer les ranges
chla_range <- range(c(df$chla, df$chla_raw), na.rm = TRUE)
depth_range <- rev(range(df$depth, na.rm = TRUE))

# Générer les graphes par profil
df %>%
  group_by(seal_id, profile_id, date_simple) %>%
  group_split() %>%
  walk(function(df_profile) {
    seal <- df_profile$seal_id[1]
    profile <- df_profile$profile_id[1]
    date <- df_profile$date_simple[1]
    
    output_dir <- file.path("oceanographic_data_corrected", DEPLOY_FOLDER, seal, "chla_fix_scale")
    if (!dir.exists(output_dir)) dir.create(output_dir, recursive = TRUE)

    if (all(is.na(df_profile$chla)) & all(is.na(df_profile$chla_raw))) return(NULL)

    p <- ggplot(df_profile) +
      geom_point(aes(x=chla, y=depth, color="Corrected"), size=0.5) +
      geom_point(aes(x=chla_raw, y=depth, color="Raw"), size=0.5) +
      geom_hline(aes(yintercept = MLD, linetype = "MLD"), color = "purple", linewidth = 0.8) +
      scale_y_reverse(name = "Depth (m)", limits = depth_range) +
      scale_x_continuous(name = "Chl-a (mg/m3)", limits = chla_range) +
      scale_color_manual(values = c("Raw" = "blue", "Corrected" = "darkgreen")) +
      scale_linetype_manual(
        name = "",
        values = c("MLD" = "dashed")
      ) +
      labs(title = paste(seal, "- Profile n°", profile, " (", date,")")) +
      theme_minimal() +
      theme(
        panel.background = element_rect(fill = "white", color = NA),
        plot.background = element_rect(fill = "white", color = NA)
      )

    filename <- paste0(seal, "_", profile, "_", date, ".png")
    filepath <- file.path(output_dir, filename)
    ggsave(filepath, plot = p, width = 6, height = 5, dpi = 300)
  })
```

# LIGHT + CHLA
## Plots light + Chla with ggplot
```{r}

library(ggplot2)
library(dplyr)
library(tidyr)
library(scales)

# Charger les données
all_seals_data <- readRDS(RDS_DEPLOYMENT_PATH)

# Préparer les données
df <- all_seals_data %>%
  dplyr::select(depth, chla = chla_corrected, light = light_corrected, date_simple, seal_id, profile_id, MLD) %>%
  filter(!is.na(chla) & !is.na(light) & !is.na(depth))

# Déterminer les ranges communs
chla_range <- range(df$chla, na.rm = TRUE)
light_range <- range(df$light, na.rm = TRUE)
depth_range <- rev(range(df$depth, na.rm = TRUE))

# petite transformation pour que l'axe de lumiere tombe juste
axis_transform <- (last(light_range) - first(light_range)) / (max(chla_range) - min(chla_range))
axis_offset <- first(light_range) - min(chla_range) * axis_transform

# Générer les graphes par profil
df %>%
  group_by(seal_id, profile_id, date_simple) %>%
  group_split() %>%
  purrr::walk(function(df_profile) {
    seal <- df_profile$seal_id[1]
    profile <- df_profile$profile_id[1]
    date <- df_profile$date_simple[1]
    
    output_dir <- file.path("oceanographic_data_corrected", DEPLOY_FOLDER, seal, "light_chla_fix_scale")
    if (!dir.exists(output_dir)) dir.create(output_dir, recursive = TRUE)

    if (diff(chla_range) == 0 || diff(light_range) == 0) {
      return(NULL)
    }
    
    # Normalisation de light pour superposition avec chla
    df_profile <- df_profile %>%
      mutate(light_rescaled = (light - axis_offset) / axis_transform)

    p <- ggplot(df_profile) +
      geom_point(aes(x = chla, y = depth, color = "chla"), size = 0.5) +
      geom_point(aes(x = light_rescaled, y = depth, color = "light"), size = 0.5) +
      geom_hline(aes(yintercept = MLD, linetype = "MLD"), color = "purple", linewidth = 0.8) +
      scale_y_reverse(name = "Depth (m)", limits = depth_range) +
      scale_color_manual(
        name = "Variable",
        values = c("chla" = "darkgreen", "light" = "goldenrod")
      ) +
      scale_x_continuous(
        name = "Chl-a (mg/m³)",
        limits = chla_range,
        sec.axis = sec_axis(
          transform =  ~ . * axis_transform + axis_offset,
          name = "Light (µmol/m²/s)"
      )) +
      scale_linetype_manual(
        name = "",
        values = c("MLD" = "dashed")
      ) +
      labs(
        title = paste(seal, "- Profile n°", profile, " (", date,")")
      ) +
      theme_minimal() +
      theme(
        axis.title.x = element_text(color = "darkgreen"),
        axis.title.x.top = element_text(color = "goldenrod"),
        axis.text.x = element_text(color = "darkgreen"),
        axis.text.x.top = element_text(color = "goldenrod"),
        panel.background = element_rect(fill = "white", color = NA),
        plot.background = element_rect(fill = "white", color = NA)
      )
    # Export
    filename <- paste0(seal, "_profile_", profile, "_", date, ".png")
    filepath <- file.path(output_dir, filename)
    ggsave(filepath, plot = p, width = 6, height = 5, dpi = 300)
   })

```

## MLD : Mixed Layer Depth
```{r}
library(ggplot2)
library(dplyr)

all_seals_data <- readRDS(RDS_DEPLOYMENT_PATH)

all_seals_data <- all_seals_data %>% mutate(deployment = sub("-.*", "", seal_id))

# Nettoyer et ordonner sans filtrer MLD ni QD
data <- bind_rows(all_seals_data) %>%
  arrange(deployment, seal_id, profile_id, date)

data$date <- as.Date(data$date)
year <- format(data$date, "%Y")

# Mixed Layer Depth (MLD) plot
ggplot(data %>% filter(!is.na(MLD)), aes(x = date, y = MLD, color = seal_id)) +
  geom_point(shape = 20, size = 0.25) +
  geom_smooth(method = "loess", formula = "y ~ x", linewidth = 0.25, span = 0.5, degree = 1,  se = T) +
  scale_y_reverse(limits = c(300, 0)) +
  scale_x_date(date_breaks = "1 month", date_labels = "%B") +
  labs(x = "", y = "Depth (m)", color = "Seal ID",
       title = paste0("Mixed Layer Depth (", DEPLOY_FOLDER, " - ", year,")")) +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))

```
### QD : Quenching depth
```{r}
# Quenching Depth (QD) plot (il faut lancer le bout de code du chunk précédent MAIS ne pas plot, le plot du MLD est long)
ggplot(data %>% filter(!is.na(QD)), aes(x = date, y = QD, color = seal_id)) +
  geom_point(shape = 4) +
  geom_smooth(method = "loess", formula = "y ~ x", linewidth = 0.25, span = 0.5, degree = 1, se = FALSE) +
  scale_y_reverse(limits = c(30, 0)) +
  scale_x_date(date_breaks = "1 month", date_labels = "%B") +
  labs(x = "", y = "Depth (m)", color = "Seal ID", 
       title = paste0("Quenching Depth (", DEPLOY_FOLDER, " - ", year,")")) +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```

## Maximum depth for temp / light / chla
```{r}
library(dplyr)
library(ggplot2)

all_seals_data <- readRDS(RDS_DEPLOYMENT_PATH)

# filtrer avec seal_id
filtered_data <- all_seals_data %>%
  filter(seal_id == SEAL_NAME)

# Derniers points température
last_temp <- filtered_data %>%
  group_by(profile_id) %>%  
  filter(!is.na(temperature)) %>%
  slice_tail(n = 1) %>%
  ungroup()

# Derniers points lumière
last_light <- filtered_data %>%
  group_by(profile_id) %>%  
  filter(!is.na(light_raw)) %>%
  slice_tail(n = 1) %>%
  ungroup()

# Derniers points chla
last_chla <- filtered_data %>%
  group_by(profile_id) %>%  
  filter(!is.na(chla_raw)) %>%
  slice_tail(n = 1) %>%
  ungroup()

# Tracer les deux types de points
ggplot() + 
  geom_point(data = last_temp, aes(x = date, y = depth), color = "blue", size = 0.5) +
  geom_point(data = last_light, aes(x = date, y = depth), color = "red", size = 0.5) +
  geom_point(data = last_chla, aes(x = date, y = depth), color = "green", size = 0.5) +
  scale_y_reverse() +
  scale_x_datetime(date_labels = "%b %Y") +
  labs(title = paste0("Maximum depth per profile - ", SEAL_NAME), x = "", y = "Depth (m)") +
  theme_minimal()


```


# MAPS
## DDU RDS Date
```{r}
library(raster)

DDU_loc=NULL
DDU_loc$lat = -66.66306	
DDU_loc$lon = 140.00083
DDU_loc=as.data.frame(DDU_loc)

min_lon <- 136
max_lon <- 146
min_lat <- -67.3
max_lat <- -65.4
# min_lon <- 139
# max_lon <- 141
# min_lat <- -66.75
# max_lat <- -66.25

# Charger les données
#all_seals_data <- readRDS(RDS_DEPLOYMENT_PATH)
# Pour savoir sur quelle date on retrouve le plus de profils
# max_date <- names(which.max(nbfoisonvoitlesdates))
# max_count <- max(nbfoisonvoitlesdates)
# cat("Date la plus fréquente :", max_date, "avec", max_count, "occurrences\n")
```
## Bathy
```{r}
library(raster)

crop <- c(min_lon, max_lon, min_lat, max_lat)
ext_crop <- extent(crop)

large_topo <- raster(BATHY_PATH_LARGE)
large_topo_spat <- terra::rast(large_topo)

large_topo_spat_crop <- crop(large_topo_spat, ext_crop)

large_topo_spat_crop[large_topo_spat_crop >=0] <-NA

```
## Fast-ice
```{r}
library(raster)
library(terra)
library(dplyr)
library(tidyr)
library(lubridate)
library(zoo)

prj <- "+proj=stere +lat_0=-90 +lat_ts=-70 +datum=WGS84"
ex <- c(-2691055, 2933945, -2390156, 2309844)

r <- rast(FAST_ICE_PATH, "Fast_Ice_Time_Series")
ext(r) <- ext(ex)
crs(r) <- prj
# plot(r)

# define the time needed (permet de savoir quels indices mettre dans raster_range)
all_seals_data <- readRDS(RDS_DEPLOYMENT_PATH)
raster_time <- time(r)
date_min <- as.Date(min(all_seals_data$date_simple, na.rm = TRUE))
date_max <- as.Date(max(all_seals_data$date_simple, na.rm = TRUE))
raster_range <- c(1,2,3,4,5,6,7,8) # Lignes à modifier pour changer de deployment SealOfApproval

crop_bounds <- c(130, 150, -67.5, -65)
pts <- cbind(lon = crop_bounds[c(1,2)], lat = crop_bounds[c(3,4)])
prj_pts <- project(pts, to = prj, from = "EPSG:4326")
ex_crop <- ext(prj_pts[2,1], prj_pts[1,1], prj_pts[2,2], prj_pts[1,2])

new_r <- crop(r, ex_crop)
# plot(new_r)

x_sub <- subset(new_r, raster_range)
# plot(x_sub)

#description: Classified surface type: 0 = pack ice or ocean; 1 = continent; 2 = islands; 3 = ice shelf; 4 = fast ice; 5 = manual fast ice edge; 6 = auto fast ice edge.
x_sub_clean <- classify(x_sub, cbind(from = c(0, 2, 5, 6), to = NA))

# On projette ...
x_sub_proj <- project(x_sub_clean, crs(large_topo_spat), res = res(large_topo_spat))
# plot(x_sub_proj)

# ... et on crop (dans ce sens là)
x_sub_proj <- crop(x_sub_proj, extent(min_lon, max_lon, min_lat, max_lat))
# plot(x_sub_proj)

# Assigner les noms de couches avec les vraies dates
raster_time <- time(r)[raster_range]
names(x_sub_proj) <- as.character(raster_time)

#----------------------------------------------------------------------------------------

# Conversion en data.frame long
x_sub2_df <- as.data.frame(x_sub_proj, xy = TRUE)
x_sub2_long <- pivot_longer(
  x_sub2_df,
  cols = -c(x, y),
  names_to = "date_simple",
  values_to = "value"
) %>%
  mutate(
    month_date = date_simple %>% ymd() %>% + days(5),
    month_label = format(month_date, "%B"),
    type = factor(month_label, levels = unique(month_label))
  ) %>%
  filter(!is.na(value))  # optionnel : retire les NA

# Créer une nouvelle colonne avec les labels
x_sub2_long$surface_type <- factor(x_sub2_long$value,
                                  levels = c(1, 3, 4),
                                  labels = c("continent", "ice_shelf", "fast_ice"))

```

## SIC
```{r}
library(raster)
library(terra)
library(stringr)
library(lubridate)
library(dplyr)
library(purrr)

# Paramètres
wgs <- "+proj=longlat +ellps=WGS84"
amsr_files <- list.files(AMSR_FOLDER_PATH, full.names = TRUE)
# Lignes à modifier pour changer de deployment SealOfApproval
# target_year_months <- c("201902", "201903", "201904", "201905", "201906", "201907", "201908", "201909") 
target_year_months <- c("202002", "202003", "202004", "202005", "202006", "202007", "202008", "202009")
# target_year_months <- c("202302", "202303", "202304", "202305", "202306", "202307", "202308", "202309") 

rasters_means <- list()

crop_bounds <- c(130, 150, -67.5, -65)

# Lire un raster pour obtenir le CRS
r_test <- raster(amsr_files[1])
crs_native <- projection(r_test)

# Convertir les coordonnées WGS84 vers le CRS
pts <- cbind(lon = crop_bounds[c(1,2)], lat = crop_bounds[c(3,4)])
pts_sp <- sp::SpatialPoints(pts, proj4string = CRS(wgs))
pts_prj <- sp::spTransform(pts_sp, CRS(crs_native))
prj_coords <- coordinates(pts_prj)

# Définir extent pour le crop initial dans le CRS natif
ex_crop <- extent(min(prj_coords[,1]), max(prj_coords[,1]),
                  min(prj_coords[,2]), max(prj_coords[,2]))

for (m in target_year_months) {
  selected_files <- amsr_files[str_detect(amsr_files, paste0("s3125-", m))]
  if (length(selected_files) == 0) next

  # Lire et traiter chaque fichier du mois
  rasters <- lapply(selected_files, function(f) {
    
    r <- raster(f)
    r <- setValues(raster(r), values(r))  # copie propre
    # crop grossier
    crop_r <- crop(r, ex_crop)
    # projection en WGS84
    r_proj <- projectRaster(crop_r, crs = wgs)
    # crop fin en WGS84
    final_crop <- extent(min_lon, max_lon, min_lat, max_lat)
    r_cropped <- crop(r_proj, final_crop)
    # Suppression valeurs aberrantes
    r_cropped[r_cropped > 100] <- NA_real_
    r_cropped
  })

  r_stack <- stack(rasters)
  mean_r <- calc(r_stack, mean, na.rm = TRUE)

  # Convertir en SpatRaster
  mean_r_terra <- rast(mean_r)

  # Ajouter au nom la date formatée
  formatted_date <- format(as.Date(paste0(m, "01"), "%Y%m%d"), "%B")
  names(mean_r_terra) <- formatted_date

  rasters_means[[formatted_date]] <- mean_r_terra
}

# Empiler tous les rasters moyens en un seul SpatRaster multitemporel
sic_stack <- rast(rasters_means)

# Convertir en data.frame long pour ggplot
df_SIC <- as.data.frame(sic_stack, xy = TRUE) %>%
  pivot_longer(
    cols = -c(x, y),
    names_to = "month_label",
    values_to = "mean"
  ) %>%
  mutate(type = factor(month_label, levels = names(sic_stack)))

```

### Plotting SIC
```{r}
library(tmap)

# Multicartes avec tmap
tm_shape(sic_stack) +
  tm_raster(style = "cont", palette = "-viridis", title = "SIC (%)") +
  tm_facets(ncol = 3) +
  tm_layout(main.title = "SIC mensuel moyen", main.title.position = "center")
```

```{r}
# Du code simplement utile pour trouver les profils TRES loins
# positions_wd12 <- all_seals_data %>%
#   filter(seal_id == "wd12-146-19") %>%
#   distinct(profile_id, lon, lat)
# 
# positions_wd12_subset <- df_maps %>%
#   filter(seal_id == "wd12-146-19", lat >= -65, lat <= -64) %>%
#   distinct(profile_id, lon, lat)
```

### Preparing facets
```{r}
library(dplyr)
library(lubridate)

df_maps <- all_seals_data %>%
  dplyr::select(seal_id, profile_id, depth, lon, lat, light_corrected, chla_corrected, date_simple) %>%
  #filter(!(seal_id == "wd12-146-19" & profile_id == 9)) %>% # SealOfApproval
  mutate(
    date_simple = as.Date(date_simple),
    day_of_month = day(date_simple),
    month_label = format(date_simple, "%B")
  ) %>%
  mutate(type = factor(month_label, levels = c("February", 
                                               "March", 
                                               "April", 
                                               "May", 
                                               "June",
                                               "July",
                                               "August",
                                               "September"))) %>% 
  filter(lon >= min_lon & lon <= max_lon, lat >= min_lat & lat <= max_lat)
# Mois à modifier pour changer de deployment SealOfApproval
```

## Plot : One month only
```{r}
library(ggplot2)
library(ggnewscale)
library(cmocean)
library(terra)
library(tidyterra)

library(dplyr)

theMonthYouWant = "May"

df_SIC_month <- df_SIC %>% filter(grepl(theMonthYouWant, month_label))
x_sub2_long_month <- x_sub2_long %>% filter(grepl(theMonthYouWant, month_label))
df_maps_month <- df_maps %>% filter(grepl(theMonthYouWant, month_label))

ggplot() +
  geom_spatraster_contour(data = large_topo_spat_crop) + # bathy

  new_scale_fill() +
  geom_raster(data = df_SIC_month, aes(x = x, y = y, fill = mean)) + # SIC
  scale_fill_cmocean(name = "ice", na.value = NA) +

  new_scale_fill() +
  geom_raster(data = x_sub2_long, aes(x = x, y = y, fill = surface_type), alpha = 0.8) +  # fast ice
    scale_fill_manual(values = c(
      "continent" = "slategray",
      "ice_shelf" = "#A9D6E5",
      "fast_ice" = "#C2B280"), na.value = NA, name = "Surface type") +
  
  new_scale_color() +
  geom_point(data = df_maps_month, aes(x = lon, y = lat, color = day_of_month), size = 0.5) +
  scale_color_viridis_c(name = "Month day", option = "C", limits = c(1, 31), breaks = seq(1, 31, 5)) +

  geom_point(data = DDU_loc, aes(x = lon, y = lat), color = "red", shape = 4, size = 2, stroke = 1) +
  annotate("text", x = DDU_loc$lon, y = DDU_loc$lat, label = "DDU", hjust = 1.5, vjust = 1.5, color = "red", size = 4) +

  theme_minimal() +
  labs(x = "Lon", y = "Lat", title = paste0(DEPLOY_FOLDER, " (", theMonthYouWant," only, colored by day)"))

```
## Plot : Facet Date map
```{r}
library(ggplot2)
library(ggnewscale)
library(cmocean)
library(terra)
library(tidyterra)

ggplot() +

  geom_raster(data = df_SIC, aes(x = x, y = y, fill = mean)) + # SIC
  scale_fill_cmocean(name = "ice", na.value = NA) +

  new_scale_fill() +
  geom_raster(data = x_sub2_long, aes(x = x, y = y, fill = surface_type), alpha = 0.8) +  # fast ice
    scale_fill_manual(values = c(
      "continent" = "slategray",
      "ice_shelf" = "#A9D6E5",
      "fast_ice" = "#C2B280"), na.value = NA, name = "Surface type") +
  
  new_scale_color() +
  # Affichage en couleur continue selon date
  geom_point(data = df_maps, aes(x = lon, y = lat, color = day_of_month), size = 0.5) +
  scale_color_viridis_c(name = "Month day", option = "C", limits = c(1, 31), breaks = seq(1, 31, 5)) +

  geom_point(data = DDU_loc, aes(x = lon, y = lat), color = "red", shape = 4, size = 1, stroke = 1) +
  annotate("text", x = DDU_loc$lon, y = DDU_loc$lat, label = "DDU", hjust = 1.5, vjust = 1.5, color = "red", size = 2) +
  
  theme_minimal() +
  facet_wrap(~type) +
  labs(x = "Lon E", y = "Lat S", title = paste0(DEPLOY_FOLDER, "(colored by day)"))

```
## Plot : Facet Wd Map
```{r}
library(ggplot2)
library(ggnewscale)
library(cmocean)

 ggplot() +
  geom_raster(data = df_SIC, aes(x = x, y = y, fill = mean), alpha = 0.7, show.legend = FALSE) +
  scale_fill_cmocean(name = "ice", na.value = NA) +
  labs(fill = "Mean SIC (%)") +
  
  new_scale_fill() +
  geom_raster(data = x_sub2_long, aes(x = x, y = y, fill = surface_type), alpha = 0.8, show.legend = FALSE) +
    scale_fill_manual(values = c(
      "continent" = "slategray",
      "ice_shelf" = "#A9D6E5",
      "fast_ice" = "#C2B280"), na.value = NA, name = "Surface type") +

  new_scale_color() +
  # Affichage en couleur continue selon individu
  geom_point(data = df_maps, aes(x = lon, y = lat, color = seal_id), size = 0.25, show.legend = FALSE) +
  scale_color_discrete(name = "seal id") +

  geom_point(data = DDU_loc, aes(x = lon, y = lat), color = "red", shape = 4, size = 1, stroke = 0.5) +
  annotate("text", x = DDU_loc$lon, y = DDU_loc$lat, label = "DDU", hjust = 1.5, vjust = 1.5, color = "red", size = 2) +
  
  facet_wrap(~type, ncol = 8, scales = "fixed") +
  labs(x = "Lon", y = "Lat", title = AMSR_FOLDER_YEAR) +
  
  theme_minimal() +
  theme(
    aspect.ratio = 1,
    axis.title = element_blank(),
    axis.text = element_blank(),
    axis.ticks = element_blank(),
    strip.text = element_blank()
    # legend.position = c(0.98, 0.05),
    # legend.justification = c("right", "bottom"),
    # legend.box = "horizontal",       # les met côte à côte
    # legend.title = element_text(size = 10),
    # legend.text  = element_text(size = 8),
    # legend.key.size = unit(0.3, "cm")
  )

```

### Plot : Wd focus on one deployment
```{r}
library(ggplot2)
library(ggnewscale)
library(cmocean)

seal_of_interest <- "wd12-146-19"
df_maps$highlight <- ifelse(df_maps$seal_id == seal_of_interest, "highlight", "other")

ggplot() +
  geom_raster(data = df_SIC, aes(x = x, y = y, fill = mean), alpha = 0.7) +
  scale_fill_cmocean(name = "ice", na.value = NA) +
  
  new_scale_fill() +
  geom_raster(data = x_sub2_long, aes(x = x, y = y, fill = surface_type), alpha = 0.8, show.legend = FALSE) +
    scale_fill_manual(values = c(
      "continent" = "slategray",
      "ice_shelf" = "#A9D6E5",
      "fast_ice" = "#C2B280"), na.value = NA, name = "Surface type") +

  new_scale_color() +
  # Points gris pour tous les autres
  geom_point(data = subset(df_maps, highlight == "other"),
             aes(x = lon, y = lat),
             color = "gray60", alpha = 0.3, size = 0.5) +
  # Point coloré pour l’individu sélectionné
  geom_point(data = subset(df_maps, highlight == "highlight"),
             aes(x = lon, y = lat, color = seal_id), size = 0.5) +

  geom_point(data = DDU_loc, aes(x = lon, y = lat), color = "red", shape = 4, size = 1, stroke = 1) +
  annotate("text", x = DDU_loc$lon, y = DDU_loc$lat, label = "DDU", hjust = -0.2, vjust = -0.5, color = "red", size = 2) +
  theme_minimal() +
  facet_wrap(~type) +
  labs(x = "Lon", y = "Lat", title = paste0(DEPLOY_FOLDER,"(",seal_of_interest," highlighted)"))

```
## Plot : Chla value facet map
```{r}
library(ggplot2)
library(ggnewscale)
library(cmocean)
library(viridis)

ggplot() +
  geom_raster(data = df_SIC, aes(x = x, y = y, fill = mean), alpha = 0.7) +
  scale_fill_cmocean(name = "ice", na.value = NA) +
  
  new_scale_fill() +
  geom_raster(data = x_sub2_long, aes(x = x, y = y, fill = surface_type), alpha = 0.8, show.legend = FALSE) +
    scale_fill_manual(values = c(
      "continent" = "slategray",
      "ice_shelf" = "#A9D6E5",
      "fast_ice" = "#C2B280"), na.value = NA, name = "Surface type") +

  new_scale_color() +
  # Points colorés selon chla_corrected
  geom_point(data = df_maps, aes(x = lon, y = lat, color = chla_corrected), size = 0.5, alpha = 0.8) +
  scale_color_viridis_c(option = "D", na.value = NA, name = "Chl-a", limits = c(0,5)) +

  geom_point(data = DDU_loc, aes(x = lon, y = lat), color = "red", shape = 4, size = 1, stroke = 1) +
  annotate("text", x = DDU_loc$lon, y = DDU_loc$lat, label = "DDU", hjust = -0.2, vjust = -0.5, color = "red", size = 2) +
  theme_minimal() +
  facet_wrap(~type) +
  labs(x = "Lon", y = "Lat", title = paste0(DEPLOY_FOLDER, "(Chl-a)"))

```
## T/S Diagram
```{r fig.height=6, fig.width=16}
library(gsw)
library(plot3D)
library(dplyr)
library(viridis)

all_seals_data <- readRDS(RDS_DEPLOYMENT_PATH)
seal_ids <- sort(unique(all_seals_data$seal_id))

x_lim <- c(33, 35)
y_lim <- c(-2.05, -1.05)
# Lignes à modifier pour changer de deployment SealOfApproval
start_time <- as.POSIXct("2020-02-01", tz = "UTC")
end_time   <- as.POSIXct("2020-09-30", tz = "UTC")
max_days <- as.numeric(difftime(end_time, start_time, units = "days"))

n_seals <- length(seal_ids)
layout(matrix(1:(n_seals+1), nrow = 1), widths = c(rep(5, n_seals), 1))

# Diagrammes T/S
par(mar = c(4, 4, 2, 1), cex.lab = 1.5, cex.axis = 1.5, cex.main = 1.5)
col_palette <- viridis(100)

for (seal in seal_ids) {
  seal_data <- all_seals_data %>% filter(seal_id == seal)
  valid <- with(seal_data, !is.na(salinity) & !is.na(temperature) & 
                  !is.na(depth) & !is.na(date) &
                  date >= start_time & date <= end_time)
  if (sum(valid) == 0) next
  
  SA <- seal_data$salinity[valid]
  CT <- gsw_CT_from_t(SA, seal_data$temperature[valid], p = seal_data$depth[valid])
  time_numeric <- as.numeric(difftime(seal_data$date[valid], start_time, units = "days"))
  
  sa_grid <- seq(x_lim[1], x_lim[2], length.out = 100)
  ct_grid <- seq(y_lim[1], y_lim[2], length.out = 100)
  sigma.c <- outer(sa_grid, ct_grid, FUN = function(S, T) gsw_sigma0(S, T))
  
  contour2D(
    x = sa_grid, y = ct_grid, z = sigma.c,
    xlim = x_lim, ylim = y_lim,
    main = seal,
    col = "black", lwd = 1.5,
    xlab = "", ylab = ""
  )
  
  scatter2D(SA, CT, colvar = time_numeric, pch = 16, cex = 0.5,
            colkey = FALSE, add = TRUE, clim = c(0, max_days),
            col = col_palette)
}

# Colorbar dans dernier panneau
par(mar = c(2, 1, 2, 2.5))
plot.new()
plot.window(xlim = c(0,1), ylim = c(0, max_days))
y_seq <- seq(0, max_days, length.out = length(col_palette)+1)
for(i in seq_along(col_palette)) {
  rect(0.2, y_seq[i], 0.8, y_seq[i+1], col = col_palette[i], border = NA)
}

axis(4, at = seq(0, max_days, length.out = 8),
     labels = format(start_time + seq(0, max_days, length.out = 8)*86400, "%b"))
mtext("2023", side = 3, line = 0.25)

# Labels globaux pour tous les plots
mtext("Salinity", side = 1, outer = TRUE, line = -1.5)
mtext("T (°C)", side = 2, outer = TRUE, line = -1.5)

```

## T/ S diagram with 100m depth range
```{r}
library(gsw)
library(plot3D)
library(dplyr)
library(viridis)

all_seals_data <- readRDS(RDS_DEPLOYMENT_PATH)

seal_ids <- sort(unique(all_seals_data$seal_id))

# Bornes fixes
x_lim <- c(33, 35)       # salinité
y_lim <- c(-2.05, -1.05)  # température

# Lignes à modifier pour changer de deployment SealOfApproval
start_time <- as.POSIXct("2019-02-01", tz = "UTC")
end_time   <- as.POSIXct("2019-09-30", tz = "UTC")
max_days <- as.numeric(difftime(end_time, start_time, units = "days"))

# Définir les tranches de profondeur
depth_bins <- list(
  "0-100 m" = c(0, 100),
  "100-200 m" = c(100, 200)
)

# Nombre de lignes = nombre de tranches de profondeur
n_row <- length(depth_bins)
# Nombre de colonnes = nombre d'individus
n_col <- length(seal_ids)
par(mfrow = c(n_row, n_col), mar = c(2, 2, 2, 2), oma = c(4, 4, 4, 2))
col_palette <- viridis(100)

for (bin_name in names(depth_bins)) {
  range <- depth_bins[[bin_name]]
  
  for (seal in seal_ids) {
    seal_data <- all_seals_data %>% filter(seal_id == seal)
    
    sal <- seal_data$salinity
    temp <- seal_data$temperature
    depth <- seal_data$depth
    time <- seal_data$date
    
    valid <- !is.na(sal) & !is.na(temp) & !is.na(depth) & !is.na(time) &
             time >= start_time & time <= end_time
    if (sum(valid) == 0) {
      plot.new(); box()
      next
    }
    
    sal <- sal[valid]; temp <- temp[valid]; depth <- depth[valid]; time <- time[valid]
    SA_all <- sal
    CT_all <- gsw_CT_from_t(SA_all, temp, p = depth)
    time_numeric_all <- as.numeric(difftime(time, start_time, units = "days"))
    
    in_bin <- depth >= range[1] & depth < range[2]
    if (sum(in_bin) == 0) {
      plot.new(); box()
      next
    }
    
    SA <- SA_all[in_bin]
    CT <- CT_all[in_bin]
    time_numeric <- time_numeric_all[in_bin]
    
    sa_grid <- seq(x_lim[1], x_lim[2], length.out = 100)
    ct_grid <- seq(y_lim[1], y_lim[2], length.out = 100)
    sigma.c <- outer(sa_grid, ct_grid, FUN = function(S, T) gsw_sigma0(S, T))
    
    contour2D(
      x = sa_grid, y = ct_grid, z = sigma.c,
      xlim = x_lim, ylim = y_lim,
      col = "black", lwd = 1.5,
      xlab = ifelse(bin_name == names(depth_bins)[n_row], expression("Salinity"), ""),
      ylab = ifelse(seal == seal_ids[1], expression("T ("*~degree*C*")"), ""),
      main = ""
    )
    
    scatter2D(SA, CT, colvar = time_numeric, pch = 16, cex = 0.5,
              colkey = FALSE, add = TRUE, clim = c(0, max_days),
              col = col_palette)
    
    # Ajouter titre de ligne (profondeur) sur la marge gauche
    if (seal == seal_ids[1]) {
      mtext(bin_name, side = 2, line = 3, at = mean(par("usr")[3:4]), cex = 1.1)
    }
    
    # Ajouter titre de colonne (individu) en haut
    if (bin_name == names(depth_bins)[1]) {
      mtext(seal, side = 3, line = 1, at = mean(par("usr")[1:2]), cex = 1)
    }
    
    # Colorbar uniquement dans la dernière case
    if (bin_name == names(depth_bins)[n_row] && seal == seal_ids[n_col]) {
      tick_vals <- seq(0, max_days, length.out = 5)
      tick_labels <- format(start_time + tick_vals * 86400, "%b")
      colkey(
        clim = c(0, max_days), dist = 0.005, side = 4, add = TRUE,
        clab = "", col.clab = "black", side.clab = 4, line.clab = 2.5,
        length = 1, width = 0.8, col.axis = "black", col.ticks = "black",
        cex.axis = 0.8, at = tick_vals, labels = tick_labels, col = col_palette
      )
    }
  }
}

# Titre global en haut
#mtext("T/S Diagrams by Seal and Depth Range (Feb 2019 - Jun 2019)", outer = TRUE, cex = 1.5, line = 1)

```

## SOON : T/S diagram with water masses
```{r}

```
